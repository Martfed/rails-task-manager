<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p>The status of this task is:
  <% if @task.completed %>
    <%= '✔'  %>
  <% else %>
    <%= '❌'  %>
  <% end %>
</p>

<p><%= link_to "Edit the task", edit_task_path(@task) %></p>
<p><%= link_to "Back to list", tasks_path %></p>
